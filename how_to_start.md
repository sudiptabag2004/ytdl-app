### How to Start the App

Open your terminal in this directory and run one of the following commands:

**Standard Mode (Best for single users / local development):**
```bash
./run.sh
```

**Production Mode (Best for multiple simultaneous users/downloads):**
```bash
./run_prod.sh
```